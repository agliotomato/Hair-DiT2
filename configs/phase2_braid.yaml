base: configs/base.yaml

training:
  phase: finetune
  dataset: braid
  resume_from: checkpoints/phase1_unbraid/best.pth

  epochs: 50
  batch_size: 4
  gradient_accumulation_steps: 2  # effective batch size 8 유지
  learning_rate: 2.0e-5
  warmup_steps: 100

  loss_weights:
    flow: 1.0
    lpips: 0.1
    lpips_warmup_frac: 0.0
    edge: 0.05

checkpointing:
  output_dir: checkpoints/phase2_braid/
  eval_every: 5
  save_every: 10
